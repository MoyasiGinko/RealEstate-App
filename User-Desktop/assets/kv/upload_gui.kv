<UploadScreen>:
    name: 'upload'

    # White background
    canvas.before:
        Color:
            rgba: 1, 1, 1, 1
        Rectangle:
            pos: self.pos
            size: self.size

    BoxLayout:
        orientation: 'vertical'
        padding: 30
        spacing: 20

        # Screen title
        Label:
            text: 'Database Management - إدارة قاعدة البيانات'
            font_size: 32
            size_hint_y: None
            height: 60
            color: 0.1, 0.1, 0.1, 1
            bold: True

        # Description
        Label:
            text: 'Manage your database: reset, backup, seed with sample data, or upload a new database file\nإدارة قاعدة البيانات: إعادة تعيين، نسخ احتياطي، إضافة بيانات تجريبية، أو رفع ملف قاعدة بيانات جديد'
            font_size: 16
            size_hint_y: None
            height: 40
            color: 0.4, 0.4, 0.4, 1
            text_size: self.width, None
            halign: 'center'

        # Spacer
        Widget:
            size_hint_y: 0.1

        # Main buttons grid
        GridLayout:
            cols: 2
            spacing: 20
            size_hint_y: None
            height: 300
            row_default_height: 140

            # Reset Database button
            BoxLayout:
                orientation: 'vertical'
                spacing: 10

                Button:
                    text: 'Reset Database\nإعادة تعيين قاعدة البيانات'
                    font_size: 18
                    background_color: 0.8, 0.2, 0.2, 1
                    color: 1, 1, 1, 1
                    size_hint_y: None
                    height: 80
                    on_press: root.create_fresh_database(self)

                Label:
                    text: 'Create a fresh empty database\n(Deletes all existing data)\nإنشاء قاعدة بيانات فارغة جديدة\n(حذف جميع البيانات الموجودة)'
                    font_size: 12
                    color: 0.5, 0.5, 0.5, 1
                    text_size: self.width, None
                    halign: 'center'
                    size_hint_y: None
                    height: 50

            # Export Database button
            BoxLayout:
                orientation: 'vertical'
                spacing: 10

                Button:
                    text: 'Export Database\nتصدير قاعدة البيانات'
                    font_size: 18
                    background_color: 0.2, 0.6, 0.8, 1
                    color: 1, 1, 1, 1
                    size_hint_y: None
                    height: 80
                    on_press: root.export_database(self)

                Label:
                    text: 'Create a backup copy of\nyour current database'
                    font_size: 12
                    color: 0.5, 0.5, 0.5, 1
                    text_size: self.width, None
                    halign: 'center'
                    size_hint_y: None
                    height: 50

            # Seed Database button
            BoxLayout:
                orientation: 'vertical'
                spacing: 10

                Button:
                    text: 'Seed Database\nإضافة بيانات تجريبية'
                    font_size: 18
                    background_color: 0.6, 0.8, 0.2, 1
                    color: 1, 1, 1, 1
                    size_hint_y: None
                    height: 80
                    on_press: root.seed_database(self)

                Label:
                    text: 'Add sample data to database\n(Preserves existing data)\nإضافة بيانات تجريبية للقاعدة\n(الاحتفاظ بالبيانات الموجودة)'
                    font_size: 12
                    color: 0.5, 0.5, 0.5, 1
                    text_size: self.width, None
                    halign: 'center'
                    size_hint_y: None
                    height: 50

            # Upload Database button
            BoxLayout:
                orientation: 'vertical'
                spacing: 10

                Button:
                    text: 'Upload Database\nرفع قاعدة البيانات'
                    font_size: 18
                    background_color: 0.8, 0.6, 0.2, 1
                    color: 1, 1, 1, 1
                    size_hint_y: None
                    height: 80
                    on_press: root.upload_database(self)

                Label:
                    text: 'Replace current database\nwith a new file'
                    font_size: 12
                    color: 0.5, 0.5, 0.5, 1
                    text_size: self.width, None
                    halign: 'center'
                    size_hint_y: None
                    height: 50

        # Spacer
        Widget:
            size_hint_y: 0.2

        # Back button
        Button:
            text: 'Back to Main - العودة للرئيسية'
            font_size: 16
            size_hint_y: None
            height: 50
            size_hint_x: 0.3
            pos_hint: {'center_x': 0.5}
            background_color: 0.6, 0.6, 0.6, 1
            color: 1, 1, 1, 1
            on_press: root.go_to_main_gui(self)